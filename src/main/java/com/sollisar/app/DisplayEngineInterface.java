package com.sollisar.app;

/** DisplayEngine interface to realize for other display methods. Looking forward to the Holoprojector Display to be implemented. */
interface DisplayEngineInterface {
    public void drawGameBoard(GameBoard b);
    public void clearDrawingArea();
}
